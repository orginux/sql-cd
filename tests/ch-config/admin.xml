<clickhouse>
    <users>
        <admin>
            <password>1212qwe</password>
            <networks>
                <ip>::/0</ip>
            </networks>

            <!-- Settings profile for user. -->
            <profile>default</profile>

            <!-- Quota for user. -->
            <quota>default</quota>

            <access_management>1</access_management>
        </admin>
        <default>
            <!-- Avoid using the default user without a password. -->
            <password>strongPass11</password>
            <access_management>0</access_management>
        </default>
    </users>
</clickhouse>
